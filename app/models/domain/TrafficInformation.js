class TrafficInformation {
  constructor(companyName, routeName, siteUrl) {
    this.companyName = companyName
    this.routeName = routeName
    this.siteUrl = siteUrl
  }
}

export default TrafficInformation
